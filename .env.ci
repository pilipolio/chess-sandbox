# CI Environment Variables
# This file is used in CI/CD pipelines to provide dummy values for testing

# Dummy OpenAI API key for testing
# Tests use respx to mock HTTP calls, so this key is never actually used
OPENAI_API_KEY=sk-dummy-key-for-ci-testing-only

# Dummy Lichess API token (not needed for unit tests)
# LICHESS_API_TOKEN=dummy-lichess-token
