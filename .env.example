# Chess Sandbox Environment Configuration

# Path to Stockfish chess engine binary
# This is required for engine analysis functionality

# macOS (Homebrew)
STOCKFISH_PATH=/opt/homebrew/bin/stockfish

# macOS (Intel Homebrew)
# STOCKFISH_PATH=/usr/local/bin/stockfish

# Linux (package manager)
# STOCKFISH_PATH=/usr/bin/stockfish

# Linux (custom install)
# STOCKFISH_PATH=/usr/local/bin/stockfish

# Docker default (set automatically in container)
# STOCKFISH_PATH=/usr/local/bin/stockfish

# Custom path
# STOCKFISH_PATH=/path/to/your/stockfish
